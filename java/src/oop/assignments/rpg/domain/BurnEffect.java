/*
package oop.assignments.rpg.domain;

public class BurnEffect extends Effect {
    private int damagePerTurn;

    public BurnEffect(int duration, int damagePerTurn) {
        super(duration);
        this.damagePerTurn = damagePerTurn;
    }

    @Override
    public void apply(java.lang.Character personage) {
        if (duration > 0) {
            personage.setScore(personage.getScore() - damagePerTurn);
            System.out.println(personage.getName() + " takes " + damagePerTurn + " burn damage.");
            reduceDuration();
        }
    }
}*/
