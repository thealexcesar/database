package oop.oop4.exercises.n7;
/**
 * Create a Speakable interface with a speak() method. Implement this interface in two
 * different classes, Dog and Cat, where each prints the sound the animal makes.
 * @author Alex Cesar
 */
public interface Speakable {
    void speak();
}
