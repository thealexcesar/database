package oop.oop4.exercises.n8;
/**
 * Create a Sortable interface with a sort(int[] array) method. Implement this interface in two different
 * classes, BubbleSort and QuickSort, which sort an array using the respective algorithms.
 * @author Alex Cesar
 */
public interface Sortable {
    void sort(int[] array);
}
